import SwiftUI

struct YellowRecordTheme {
    static let palette = RecordThemePalette(
        recordColor: Color(red: 0.86, green: 0.76, blue: 0.30),
        trackBufferColor: Color(red: 0.72, green: 0.64, blue: 0.30)
    )
}
